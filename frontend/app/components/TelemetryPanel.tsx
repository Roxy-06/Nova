"use client";
import { useEffect, useRef, useState } from "react";
import type { RejectedCandidate, TelemetryLog } from "./types";
const rejected: RejectedCandidate[] = [{ id:"r1", title:"Generic Python tutorial promises instant AI productivity", score:3, reason:"Generic tutorial; outside NOVA's frontier technology domain." }, { id:"r2", title:"New model launch repeats yesterday's benchmark claims", score:4, reason:"88% semantic overlap with a post evaluated in the past 12 hours." }, { id:"r3", title:"Consumer gadget roundup", score:2, reason:"Promotional coverage with no durable operational implication." }];
const initialLogs: TelemetryLog[] = [{id:"l1",timestamp:"22:01:04",category:"INGEST",message:"RSS Loop: Ingested 14 candidates from primary technology feeds"},{id:"l2",timestamp:"22:01:08",category:"SCORE",message:"Persona Engine: Evaluated candidate → Match Score: 92/100"},{id:"l3",timestamp:"22:01:10",category:"MEMORY",message:"Vector Memory: Deduplication passed (99.4% unique vs. past 48h posts)"},{id:"l4",timestamp:"22:01:12",category:"PUBLISH",message:"Pipeline: Post published successfully to Feed"}];
const rotating = ["Source verifier: canonical URL confirmed", "Persona Engine: impact analysis attached", "Memory layer: prior narrative link identified", "RSS Loop: source freshness window passed"];
export function TelemetryPanel({open,onClose}:{open:boolean;onClose():void}) { const [tab,setTab]=useState<"logs"|"rejected">("logs"); const [simulating,setSimulating]=useState(true); const [logs,setLogs]=useState(initialLogs); const terminal=useRef<HTMLDivElement>(null); useEffect(()=>{if(terminal.current) terminal.current.scrollTop=terminal.current.scrollHeight;},[logs,tab]); useEffect(()=>{if(!simulating)return; const id=setInterval(()=>setLogs(current=>[...current.slice(-17),{id:crypto.randomUUID(),timestamp:new Date().toLocaleTimeString("en-GB",{hour12:false}),category:["INGEST","SCORE","MEMORY"][Math.floor(Math.random()*3)] as TelemetryLog["category"],message:rotating[Math.floor(Math.random()*rotating.length)]}]),3600); return()=>clearInterval(id);},[simulating]); return <aside className={`telemetry ${open?"telemetry-open":""}`}><div className="telemetry-head"><div><p className="eyebrow">NOVA NODE / LIVE TRACE</p><h2>AGENT TELEMETRY<br/><em>THOUGHT MATRIX</em></h2></div><button className="close-panel" onClick={onClose}>×</button></div><div className="telemetry-tabs"><button className={tab==="logs"?"active":""} onClick={()=>setTab("logs")}>SYSTEM LOGS</button><button className={tab==="rejected"?"active":""} onClick={()=>setTab("rejected")}>CUTTING ROOM FLOOR</button></div>{tab==="logs"?<><div className="simulation-row"><span><i/> LIVE STREAM</span><button onClick={()=>setSimulating(v=>!v)}>{simulating?"PAUSE SIMULATION":"RESUME SIMULATION"}</button></div><div className="terminal" ref={terminal}>{logs.map(log=><p key={log.id}><time>[{log.timestamp}]</time> <b>{log.category}</b> {log.message}</p>)}</div></>:<div className="rejected-list">{rejected.map(item=><article key={item.id}><span>REJECTED BY EDITORIAL RULE</span><h3>{item.title}</h3><p><b>SCORE {item.score}/10:</b> {item.reason}</p></article>)}</div>}</aside>; }
